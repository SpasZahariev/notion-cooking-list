[project]
name = "notion-cooking-list"
version = "0.1.0"
description = "Automate weekly mealprep: pick random recipes, mark as Todo, populate shopping list"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "requests",
    "python-dotenv",
]
